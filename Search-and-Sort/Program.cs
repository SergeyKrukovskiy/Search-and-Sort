using System;

namespace Search_and_Sort
{
    class Program: SearchAndSort
    {
        static void Main()
        {
            int[] arr = new int[] { 3, 5, 1, 6, 8, 2 };
            int key = 8;
            //BubbleSort(arr,'+');
            //ChoiseSort(arr,'-');
            //InsertSort(arr, '+');
            //CountSort(arr,'-');
            //RadixSort(arr, '+');
            //Console.WriteLine(LineSearch(arr, key));
            //Console.WriteLine(BinarySearch(arr, key));
            //Console.WriteLine(LeftBinarySearch(arr, key));
            //Console.WriteLine(RightBinarySearch(arr, key));
            //Console.WriteLine(JumpSearch(arr, key));
            //Console.WriteLine(LeftJumpSearch(arr, key));
            //Console.WriteLine(RightJumpSearch(arr, key));
        }
    }
}
